.DS_Store
node_modules
.env.local
.env*.local
.next
out
tsconfig.tsbuildinfo

# local dev state store (lib/state-store.ts file fallback) — never track board data
.data/

# .claude/ is session/routine bookkeeping — never track it, so a stray
# add/commit can't sweep it in. launch.json is the one deliberate exception.
.claude/*
!.claude/launch.json

__pycache__/
*.pyc
